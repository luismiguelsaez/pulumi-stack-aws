name: network
runtime:
  name: python
  options:
    virtualenv: venv
description: AWS network resources

config:
  org: luismiguelsaez

  aws:region: eu-central-1

  network:name_prefix: "main"
  network:cidr: "10.0.0.0/16"
  network:azs: 3
  network:subnet_mask: 24
  network:ngw_multi_az: False
